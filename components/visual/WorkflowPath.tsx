'use client'

/**
 * Legacy export — process stage now uses ProcessDeliveryVisual.
 * Kept so BrandStage diagram="workflow" still resolves.
 */
export { ProcessDeliveryVisual as WorkflowPath } from '@/components/visual/ProcessDeliveryVisual'
